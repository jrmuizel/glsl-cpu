#set -e
rm -f all.h
COUNT=0
for i in *.pp; do
cargo run --manifest-path=../../glsl-to-cxx/Cargo.toml --release $i > $i.h
COUNT=$((COUNT+1))
echo "namespace k$COUNT {\n#include \"$i.h\"\n}" >> all.h
done
