#set -e
rm -f all.h
COUNT=0
for i in *.pp; do
cargo run --manifest-path=../../glsl-to-cxx/Cargo.toml --release $i > $i.h
COUNT=$((COUNT+1))
echo -e "namespace k$COUNT {\r#include \"$i.h\"\n}" >> all.h
done
